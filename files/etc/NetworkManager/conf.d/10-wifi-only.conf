# --split-network: the cable stays with systemd-networkd; NetworkManager only
# manages Wi-Fi, which is what Omarchy's network panel talks to (nmcli).
[keyfile]
unmanaged-devices=interface-name:en*;interface-name:eth*
